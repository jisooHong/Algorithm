```python3
import sys
"""
누적합
"""
def checkMin(result,i):
    global answer
    global index
    if min(answer,result) == result:
        answer = min(answer,result)
        # index = i+1

n,h = map(int,sys.stdin.readline().split())

warning = [0 for _ in range(h)]

answer = n+1
index = 0

for i in range(n):
    size = int(sys.stdin.readline())
    if i % 2 == 0: # 짝수번째일경우
        # 밑부터 채워짐
        for j in range(h-1,h-1-size,-1):
            warning[j] += 1
    else: # 홀수번째일경우
        # 위부터 채워짐
        for j in range(0,size):
            warning[j] += 1

    
# print(warning)
answer = min(warning)
cnt = 0
for i in warning:
    if answer == i:
       cnt+=1

print(str(answer) + " " + str(cnt))
```
--다시풀기
